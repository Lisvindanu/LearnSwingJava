package GUI;

import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {
        // Jframe = a GUI window to add components to
        /*
        JFrame frame = new JFrame();//creates a frame
        frame.setTitle("JFrame tittle goes here");//Sets tittle of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out of apps
        frame.setResizable(false);//prevent frame from being resized
        frame.setSize(420, 420); // set x dan y dimension
        frame.setVisible(true);//make frame visible

        ImageIcon image = new ImageIcon("src/GUI/Logo.png");//create an ImageIcon
        frame.setIconImage(image.getImage());//change icon of frame
        frame.getContentPane().setBackground(Color.pink); //change color of background
    */
        MyFrame myFrame = new MyFrame();
        }
    }
